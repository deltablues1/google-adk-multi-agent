# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
venv/
env/
ENV/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment variables
.env
.env.local
.env.*.local
deploy/rpi/.env.rpi

# ============================================================================
# SECURITY: Certificates & Keys (NEVER commit)
# ============================================================================
*.p12
*.pfx
*.pem
*.cer
*.crt
*.key
client_cert.*
client_key.*
*_ca_bundle.*

# ============================================================================
# SECURITY: Google Credentials (NEVER commit)
# ============================================================================
*.json
!package.json
!dashboards/*.json
!data/**/*.json
!schemas/**/*.json
!firestore.indexes.json

# Specific credential files (double safety)
oauth_client_credentials.json
service_account_key.json
credentials.json
service-account.json

# OAuth tokens
tokens.json
.google_workspace_adk/

# ============================================================================
# Logs & Test Output
# ============================================================================
*.log
logs/
.cache/

# Test outputs
*.txt
!requirements.txt
!requirements-dev.txt
!requirements-rpi.txt
tests/test_results*.json
tests/pts_results/

# ============================================================================
# Generated Output
# ============================================================================
output/invoices/*.pdf

# ============================================================================
# Reference Data (large binaries, not code)
# ============================================================================
*.xlsx
*.pdf

# ============================================================================
# Testing
# ============================================================================
.pytest_cache/
.coverage
htmlcov/
.tox/

# Database
*.db
*.sqlite3

# Node modules (for MCP servers)
node_modules/

# Session data (runtime data, not code)
*.session
sessions/*.session
sessions/data/

# ============================================================================
# Temporary & Garbage Files
# ============================================================================
*.tmp
*.temp
.tmp/
tmpclaude-*/
nul
# Stray pip artifacts from `pip install pkg =X.Y` (space before version)
=*
*.backup

# ============================================================================
# Runtime & Uploads
# ============================================================================
uploads/

# ============================================================================
# Claude Code
# ============================================================================
.claude/

# Docker
.dockerignore

# Cloud deployment
.gcloudignore

# OS
Thumbs.db

# ============================================================================
# Stale Directories
# ============================================================================
deploy_clean/

# RPi deployment secrets
deploy/rpi/*.env

# TTS probe artifacts
tmp_tts_*.pcm
tmp_tts_*.wav
test_results_*.log
config/tv_apps.json
config/tv_channels.json
deploy/tv_app_launcher/build/
deploy/tv_app_launcher/android.jar
deploy/tv_app_launcher/r8.jar
deploy/tv_app_launcher/*.keystore

# Learned mail recipients (real email addresses — personal data, not config).
data/known_recipients.json

# Daily model-spend ledger (local accounting, not config).
data/llm_budget.json

# Deferred voice jobs and scheduled-run outcomes (runtime state, not config).
data/background_jobs.json
data/scheduled_jobs_results.json
