# ==========================================
# RPi 5 Smart Home Deployment
# ==========================================
# Subset of requirements.txt for rpi-home profile.
# Excludes: weasyprint, playwright, google-ads, datasets

# Google AI Development Kit
google-adk>=0.1.0
google-genai>=0.3.0

# Multi-provider model routing (Claude via ADK LiteLlm). Only needed when
# LLM_PROVIDER=anthropic.
litellm>=1.40.0

# OpenAI SDK — optional OpenAI voice engine (STT_ENGINE/TTS_ENGINE=openai).
# Also a litellm dependency, pinned here so the Pi voice path is explicit.
openai>=1.40.0

# Google API Clients
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# Google Cloud
google-cloud-secret-manager>=2.16.0
google-cloud-firestore>=2.13.0
google-cloud-logging>=3.5.0
google-cloud-aiplatform>=1.43.0
google-cloud-storage>=2.14.0

# MCP (Model Context Protocol)
mcp>=0.1.0

# Web Scraping (lightweight only)
beautifulsoup4>=4.12.0
requests>=2.31.0

# Async Support
aiohttp>=3.9.0

# Database
sqlalchemy>=2.0.0

# Scheduling
APScheduler>=3.10.0

# MQTT (Smart Home)
paho-mqtt>=2.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
pytz>=2023.3
markdown>=3.5.0

# Web Interface
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sse-starlette>=1.8.0

# Telegram Bot
python-telegram-bot>=21.0

# PDF Processing (basic)
pypdf>=3.17.0
reportlab>=4.0.0

# XML Processing & Signing
lxml>=5.0.0
cryptography>=41.0.0

# QR Code Generation
qrcode[pil]>=7.4.0
Pillow>=10.0.0

# ==========================================
# RPi-specific: Wake Word & Audio
# ==========================================
# Default engine: openWakeWord (Apache-2.0, free, offline, no access key).
# Selected via WAKEWORD_ENGINE=openwakeword (default) in scripts/run_wakeword.py.
openwakeword>=0.6.0
onnxruntime>=1.17.0
sounddevice>=0.4.6
numpy>=1.24.0
# Optional alternative engine — set WAKEWORD_ENGINE=porcupine and provide
# PICOVOICE_ACCESS_KEY. Freemium (not open-source); install manually if needed:
# pvporcupine>=3.0.0

# Scraper fallback for pages Jina Reader cannot read (403, JS-rendered).
# The key is already in .env; without the package the tool fails on every call.
firecrawl-py>=4.0
